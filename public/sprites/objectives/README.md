# Objective Sprites

Drop generated transparent objective art here.

- `central_crystal.png` replaces the Crystal Rush center crystal objective.
- Import GPT Images chroma-key output with:

```bash
node scripts/import-objective-asset.mjs central_crystal /path/to/generated-crystal.png --chroma
```
